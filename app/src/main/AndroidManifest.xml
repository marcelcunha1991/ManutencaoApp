<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.maptechnology.manutencaoapp">

    <uses-permission
        android:name="android.permission.INTERNET"
        android:required="true" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.hardware.camera" />
    <uses-permission android:name="android.hardware.camera.autofocus" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.CriarCalendarioActivity"></activity>
        <activity android:name=".activities.CalendarActivity" />

        <meta-data
            android:name="com.google.android.gms.vision.DEPENDENCIES"
            android:value="barcode" />

        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.AtividadesDiaActivity" />
        <activity android:name=".activities.LoginWhirlpool" />
        <activity android:name=".activities.DetalheCalendarioActivity" />
        <activity android:name=".activities.QrCodeReaderActivity" />
        <activity
            android:name=".qrcodereader.barcode.BarcodeCaptureActivity"
            android:label="Read Barcode" />
    </application>

</manifest>